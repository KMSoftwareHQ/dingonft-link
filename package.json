{
  "name": "dingonft-link",
  "private": true,
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "start": "node app",
    "dev": "NODE_ENV=development nodemon app.js",
    "dev:storage": "docker compose up -d",
    "dev:storage:down": "docker compose down",
    "dev:storage:reset": "docker compose down -v",
    "dev:seed": "docker compose run --rm seed",
    "dev:fixtures": "python3 dev/generate-fixtures.py"
  },
  "dependencies": {
    "abort-controller": "^3.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.17.3",
    "node-fetch": "2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
